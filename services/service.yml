apiVersion: v1
kind: Service
metadata:
  name: my-nginx-service
  labels:
    app: nginx-service
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
